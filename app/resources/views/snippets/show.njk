{% extends 'layouts/default.njk' %}

{% block body %}

  <div class="wrapper">

    {% include 'partials/nav.njk' %}
    {% include 'partials/snippets.njk' %}

    <sction class="snippet-content">
      <div class="actions">
        <a href="#" class="edit" data-modal-open="#edit-snippet">Editar</a>

        <form action="/app/categories/{{categoryId}}/snippets/{{snippet.id}}?_method=DELETE" method="POST">
          <button type="submit">Remover</button>
        </form>
      </div>

      <h1>{{snippet.title}}</h1>
      <div class="content">{{snippet.formattedContent | safe }}</div>
    </sction>

    <div class="modal" id="edit-snippet">
      <form class="modal-content" method="POST" action="/app/categories/{{categoryId}}/snippets/{{snippet.id}}?_method=PUT">
        <input type="text" name="title" value="{{ snippet.title }}">


        <div class="content-container">
          <textarea name="content" class="mde" placeholder="Conteúdo do Snippet">{{ snippet.content }}</textarea>
        </div>
        <button type="submit">SALVAR SNIPPET</button>
        <a href="#" data-modal-close="#edit-snippet"> Cancelar</a>
      </form>
    </div>

  </div>

{% endblock body %}
